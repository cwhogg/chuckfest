# Supabase Configuration
# Get these values from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Resend (email)
# Get your API key from https://resend.com
RESEND_API_KEY=your-resend-api-key

# Email Test Mode
# Set to "true" to enable test mode - emails will only be sent to EMAIL_TEST_RECIPIENT
# The cron job will log which members WOULD have received emails without actually sending
# Useful for testing without a verified domain in Resend
EMAIL_TEST_MODE=false
EMAIL_TEST_RECIPIENT=your-email@example.com

# Vercel Cron
# Generate with: openssl rand -hex 32
# Also add this to Vercel environment variables
CRON_SECRET=your-random-secret-here
